{
  "name": "ringcentral-js-data-generator",
  "version": "0.1.0",
  "description": "A set of node command line applications calling Ringcentral js apis repeatedly to generate Ringcentral data records (messages, faxes, pager messages, phone call logs, call recordings). The api calls are configured by json files in `conf` directory.",
  "scripts": {
    "install": "cp -nr conf-sample/ conf;exit 0"
  },
  "dependencies": {
    "form-data": "^1.0.0-rc4",
    "http-status-codes": "^1.0.6",
    "ringcentral": "^2.0.6"
  }
}
